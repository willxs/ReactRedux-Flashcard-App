import { StyleSheet } from 'react-native'

export const fonts = StyleSheet.create({
    default: { fontSize: 24 },
    alternate: { fontSize: 50, color: "#FFFFFF" },
    large: {fontSize: 32, alignSelf: "center" }
})

export const scalingFactors = { default: 15, large: 10}